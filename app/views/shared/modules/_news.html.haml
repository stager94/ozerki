.block-title 
	%i.icon-star.icon-white{style: "margin-top: 4px;"}
	Актуальные новости
-news.each do |_new|
	= link_to (category_news_news_path(_new.category, _new)), class: "news-link" do 
		= _new.title

	.news-date= Russian::strftime(_new.created_at, "%d %B %Y")
	.news-description= truncate _new.snippet, length: 100, separator: ' '